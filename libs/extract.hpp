#include <string>
#include <vector>
std::string extract(std::string csv_line, int column_index, std::string delimiter); 
std::vector <std::string> extract2(std::string inputLine,std::string delimiter);
std::string extract3(std::string inputLine,std::string delimiterm,int n);
